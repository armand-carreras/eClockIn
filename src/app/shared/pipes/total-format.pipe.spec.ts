import { TotalFormatPipe } from './total-format.pipe';

describe('TotalFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
