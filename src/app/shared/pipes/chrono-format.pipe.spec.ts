import { ChronoFormatPipe } from './chrono-format.pipe';

describe('ChronoFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChronoFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
